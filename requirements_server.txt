# CE7454 Face Parsing - Server Requirements
# For CUDA-enabled server

# PyTorch with CUDA (install via conda)
# conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia

# Core dependencies
numpy>=1.21.0
pillow>=8.0.0

# Optional but recommended
matplotlib>=3.5.0
tqdm>=4.65.0

# For visualization (optional)
tensorboard>=2.11.0
